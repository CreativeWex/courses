# 0. Конкурентность и параллелизм

**Конкурентность (concurrency)** -- это про композицию нескольких независимых задач, которые логически выполняются одновременно, даже если на железе всего одно ядро

**Параллелизм (parallelism)** -- физическое одновременное выполнение на нескольких ядрах

> Когда не стоит использовать многопоточность?
> - Когда асинхронные операции проще и дешевле
> - При небольшом количестве ресурсов (CPU-bound на одном ядре (контекст-свитчинг > выигрыша))

Основные задачи многопоточности

- **I/O‑bound задачи** -- когда основной поток много чего-то ожидает
(долгие операции, связанные с сетью, диском, БД), несколько потоков позволяют перекрывать ожидание I/O работой других задач, повышая пропускную способность.
- **CPU‑bound задачи** -- использование нескольких потоков имеет смысл, если можно распараллелить вычисления и у сервера несколько ядер; иначе накладные расходы на контекст‑свитчинг могут только ухудшить ситуацию.
- **Отдельные кейсы** -- responsive UI / REST: перенос тяжёлой логики с основного потока (UI‑thread, request‑handling thread) в пул рабочих потоков позволяет не замораживать интерфейс и не блокировать обработку других запросов.

# 1. Базовые основы и модель памяти 🟥
## 1.1. Поток как сущность 🟥

**Процесс** -- это запущенная программа как целое.
У процесса есть своя отдельная память,
системные ресурсы (файлы, сокеты), и операционная система
видит его как независимую сущность: если один процесс упал,
другие обычно продолжают работать.

**Поток** -- это линия выполнения внутри процесса.
Несколько потоков в одном процессе разделяют одну и ту же память
и ресурсы, но каждый поток выполняет свою часть работы; так один процесс может параллельно делать несколько задач (например,
один поток читает с сети, другой пишет в базу, третий обрабатывает запрос).

> Проще понять разницу между потоком и процессом можно на реальном
> примере.
>
>JVM, на которой работают наши приложения - это процесс. Когда ты запускаешь
> Java-приложение, операционная система создает процесс, в котором загружается
> и выполняется JVM. Этот процесс имеет свое адресное пространство, дескрипторы файлов, память,
> которые JVM получает от операционной системы.
> (Памятка - запущенные потоки можно просмотреть через `ps aux` или `top`)
> 
> Внутри процесса JVM поднимаются потоки (например, main-поток, GC, служебные и те, которые мы создаем в коде) - они
> разделяют ресурсы процесса (все потоки делят между собой heap java-приложения)

**Deamon-потоки (демоны)** -- используется для фоновых задач (например, логирование, метрики),
но не для работы, где требуется гарантированное завершение. Демон
автоматически завершается вместе с завершением процесса

🟥 Класс Thread - статические методы, объект.

## 1.2 Жизненный цикл потока 🟩
У потока есть жизненный цикл

> - Возможные состояния потоков можно посмотреть в Thread.State
> - Thread.activeCount() - считает только пользовательские потоки + main, не считает системные
> - Thread.getAllStackTraces() - возвращает все потоки JVM, включая системные

- start() -- переводит поток из NEW в RUNNABLE и передаёт его планировщику.
- join() -- позволяет одному потоку подождать завершения другого.
- interrupt() -- устанавливает флаг прерывания; корректный код периодически проверяет флаг или ловит InterruptedException и корректно завершается.

🟩 Демо - Thread LifeCycle

Полный жизненный цикл потока Thread в Java:

`NEW → RUNNABLE ↔ BLOCKED → WAITING/TIMED_WAITING → TERMINATED`
1. **Создание потока - NEW**
   ```java
    Thread thread = new Thread("demo");
   ```
- Память: Объект Thread только в heap. В стеке main — ссылка на объект
- Стек потока: НЕ выделен (~0 байт)
- ОС: Не знает о потоке
- isAlive(): false
- ThreadGroup: Видна ("main")

2. **Запуск потока - RUNNABLE - готов к выполнению или выполняется**
   ```java
   thread.start();
   ```
- Память: Объект в heap + стек ОС (~1MB)
- ОС: Создан platform thread, передан планировщику
- isAlive(): true
- ThreadGroup: null (JVM очищает ссылку)
- Параллельность: Выполняется одновременно с main!

3. **Блокировка - BLOCKED**
   ```java
    synchronized(lock) { /* ожидание монитора */ }
   ```
- Что происходит: Поток ждёт освобождения монитора synchronized
- CPU: Не получает кванты времени
- Стек: Существует, содержит stack trace до synchronized

4. **Ожидание - WAITING**
   ```java
   lock.wait();           // БЕСКОНЕЧНО
   Thread.join();         // Ждёт завершения другого потока
   Lock.lockInterruptibly(); // Блокирующий lock
   ```

- Что происходит: Поток спит бесконечно до notify()/interrupt()
- Разбудить: notify()/notifyAll() или interrupt()

5. **Ожидание с таймаутом - TIMED_WAITING**
   ```java 
   Thread.sleep(1000);           // Спит 1 сек
   lock.wait(1000);              // Ждёт notify 1 сек  
   Thread.join(1000);            // Ждёт поток 1 сек
   Lock.tryLock(1, TimeUnit.SEC) // Пробует взять lock 1 сек
   ```
- Разбудить: Таймаут или notify()/interrupt()

6. **Завершение - TERMINATED - run отработал**
- Память: Стек ОС освобождён. Объект Thread в heap → GC
- isAlive(): false
- Перезапуск: Невозможен! start() → IllegalThreadStateException

## 1.2. Java Memory Model 🟥

Java Memory Model: понятия visibility, reordering, happens-before,
что реально гарантирует synchronized, volatile, final‑поля.

---

# 2. Примитивы синхронизации и проблемы 🟥

## 2.1. Проблемы многопоточности 🟥

Race condition, critical section, thread safety, что такое shared mutable state и
как его минимизировать (immutability, ThreadLocal)

synchronized (методы/блоки), intrinsic locks, monitor, reentrancy,
проблемы coarse‑grained locking.

volatile: где помогает, где нет; почему не заменяет lock, типичные misuse‑паттерны

Классические проблемы: deadlock, livelock, starvation, priority inversion, false sharing и как их диагностировать
(thread dump, профилировщики).

---

# 3. java.util.concurrent: ядро 🟥

## 3.1. Executor framework 🟥
Executor, ExecutorService, ThreadPoolExecutor, ScheduledExecutorService, политика очередей и rejection.

## 3.2. Задачи 🟥
Runnable, Callable, Future, правильное завершение пула (shutdown, awaitTermination), обработка исключений в задачах.

## 3.3 Высокоуровневые синхронизационные примитивы: 🟥

ReentrantLock, ReadWriteLock, StampedLock — когда они лучше synchronized.
Координация: CountDownLatch, CyclicBarrier, Phaser, Semaphore, паттерны «ожидание N задач», «батчи».

---

# 4. Коллекции, атомики и обмен данными 🟥
Concurrent collections: ConcurrentHashMap, ConcurrentLinkedQueue/Deque, CopyOnWriteArrayList и их типичные use‑cases.

Blocking очереди: BlockingQueue, ArrayBlockingQueue, LinkedBlockingQueue, SynchronousQueue, DelayQueue — реализация producer–consumer, пайплайнов.

Atomics и CAS: AtomicInteger/Long/Reference, LongAdder, AtomicStampedReference, когда атомики лучше/хуже локов.

ThreadLocal: где оправдан, где приводит к memory leak (особенно в контейнерах/ThreadPool).

---

# 5. Асинхронность, параллелизм и современные подходы 🟥

## 5.1. CompletableFuture 🟥
цепочки, комбинирование, allOf/anyOf, обработка ошибок, кастомные executors; построение асинхронных пайплайнов.

## 5.2 Parallel Streams и ForkJoin 🟥

ForkJoinPool, work‑stealing, RecursiveTask, где имеет смысл и как не убить latency в проде.

Ограничения parallel streams в web‑сервисах, влияние на общий пул.

Structured concurrency (Project Loom/современные практики): идея «structured» задач, cancelation propagation, связь с observability.

---

# 6. Виртуальные потоки 🟥

---

# 7. Паттерны, дизайн и best practices 🟥

Producer–Consumer, Work Stealing, Thread Pool, Read–Write lock, async pipeline, fan‑out/fan‑in.

Тайм‑ауты, backpressure, circuit breaker (в привязке к конкаренси).